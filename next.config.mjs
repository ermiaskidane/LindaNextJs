/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'utfs.io',
      'res.cloudinary.com',
      'placehold.co',
      'people.pic1.co',
      'app-uploads-cdn.fera.ai',
    ],
  },
}

export default nextConfig
